package com.example.webConf.config.message;

public enum MessageType {
    CHAT,
    CHAT_FILE,
    CONFERENCE_INVITATION,
    CHAT_INVITATION,
    JOIN,
    LEAVE,
    DELETE,
    CLEAR,
    CHAT_DELETED,
    FIRST_JOIN
}
